I’ll check the audit claims against the cited code and current dirty diff, then rank concrete plan corrections. I’ll keep this read-only; no ledger updates, live harness calls, or file changes.

The biggest issues so far are semantic: E4’s substring match can accept a different model, S1’s reused predicate still admits invalid native roots, and E5’s unconditional reset can discard Grok’s final answer. B3 also changes an allowlist without wiring any non-Codex callers, so it would not deliver the promised cooldown behavior.

**Completion report**
- **Status:** completed.
- Reviewed PLAN.md, all eleven audits, cited implementation paths, and the four-file dirty diff. No files changed; no untracked files found.
- Read-only Python replays confirmed schema-predicate gaps, ineffective text-error fallback, missed compaction failure, and the proposed Grok-reset failure. Also checked E4’s matching examples.
- No full suite or live harness calls ran. Vendor claims below remain audit evidence unless explicitly described as locally checked.

## Ranked punch list

1. **B1 — change. Do not label the expanded Devin range “verified-good.”** The existing gate explicitly protects against accepted-but-ineffective permission flags (`src/delegate_agent/cli.py:516-521`). The originating audit requires the behavioral probe **before** widening, not afterward (`docs/audits/2026-09-07-harness-compat/devin.md:44-48`). Current documentation proves flag availability, not denial behavior across every 3000.5/6 patch. **Plan edit:** “Retain the existing gate until the read-only behavioral probe passes on the target release; record documentation compatibility separately. Unavailable binary means this acceptance condition remains blocked.”

2. **B2 — change. Specify the mount mechanism, not just “rw-bind it.”** `build_bwrap_argv` deduplicates mounts by destination (`src/delegate_agent/sandbox_bwrap.py:392-397`), so adding a writable bind after the existing read-only `.kimi-code` bind can silently leave it read-only. Writable-root intersection checks also currently know only the existing engine-home overrides (`:486-495`). **Plan edit:** “Resolve `KIMI_CODE_HOME`, otherwise `$HOME/.kimi-code`; include that resolved path in writable-root validation, exclude it from read-only candidates when writable, and mount it exactly once. Test default and overridden homes, workspace intersection, and absence of other engines’ writable homes.”

3. **E4 — change. Substring matching defeats pinned continuity and still misses real Cursor names.** The proposed rule accepts `gpt-5.4` versus `gpt-5.4-mini`, yet rejects `cursor-grok-4.6-xhigh` versus `Cursor Grok 4.6 Extra High`. Both results were checked locally. Cursor already supplies the correct ID/display-name mapping (`src/delegate_agent/harness_discovery.py:1037`; `docs/audits/2026-09-07-harness-compat/cursor.md:79-82`). The existing comparison guards identity, not approximate similarity (`src/delegate_agent/harness_events.py:659-679`). **Plan edit:** “Use exact IDs plus engine-specific, evidenced equivalents: Cursor catalog display names; explicitly recognized Claude aliases/canonical IDs. No generic containment. Unresolvable dynamic aliases must fail pinned preflight or explicitly select fungible mode. Preserve exact mid-run identity checks.”

4. **S1 — change. `_is_object_node` is the wrong native-root predicate.** It accepts object/null unions, type-less `properties`, and even `{"type":"array","properties":{}}` (`src/delegate_agent/structured_output.py:30-37`); replay confirmed all three. It identifies nodes needing object normalization, not schemas restricted to object instances. The audits themselves conflict on this point; shared B6 identifies precisely these gaps (`docs/audits/2026-09-07-harness-compat/shared.md:70-79`). **Plan edit:** “Native workflow eligibility requires explicit root `type == "object"`; retain `_is_object_node` for recursive normalization. Other roots use prompt-and-parse without altering their semantics. Apply the Claude byte budget to the actual serialized argv value.”

5. **S1/S2 — change. Cover direct schema requests and make demotion explicit.** Workflow-only gating leaves direct Claude `--output-schema` requests unchanged: their contents are read and forwarded without equivalent preflight (`src/delegate_agent/request_build.py:2959-2977`), while Codex has a separate preflight (`:530-552`). Also, `child.text is None` does not establish schema rejection. After demotion, a resumable retry currently receives only `_structured_resume_prompt(prior_error)` and skips embedding the schema (`src/delegate_agent/workflows/runtime.py:2926-2936,2980-2982`). **Plan edit:** “Share engine-native eligibility checks across workflow and direct request boundaries; direct unsupported native requests fail clearly. Demote workflow retries on identified native-schema rejection, not arbitrary timeout/auth failure. On the first demoted attempt, always include the original schema, including when resuming; journal the reason.”

6. **E5 — change/add. Preserve the final response across `usage`, and handle the omitted truncation event.** `_ingest_grok_end` consumes only `_grok_text_buffer` (`src/delegate_agent/harness_events.py:1004-1028`). Literally clearing that buffer at the final response’s `usage` produces a succeeded terminal with no completion; replay confirmed this. Grok L4’s independent `max_turns_reached` signal is also absent from the plan (`docs/audits/2026-09-07-harness-compat/grok.md:122-124`); `_provider_terminal_state` does not inspect the event type for that condition (`src/delegate_agent/harness_events.py:352-379`). **Plan edit:** “At `usage`, seal the response candidate and reset only the active buffer; promote the final response at `end`, never an earlier tool preamble. Test single-response and multi-response streams with final `usage`. Map `max_turns_reached` alongside `max_turn_requests`.”

7. **E6 — change. Renaming the compaction events is insufficient.** The existing end handler requires `_pi_recovery_error` before recognizing compaction failure (`src/delegate_agent/harness_events.py:1197-1203`). A standalone aborted compaction with `errorMessage` therefore remains unclassified after the proposed spelling fix; replaying the equivalent existing spelling confirmed it. **Plan edit:** “Accept both dialects, but also recognize an explicit failed/aborted compaction without requiring a preceding provider error. Preserve successful/manual-compaction behavior and retry recovery. Test aborted compaction both with and without prior failure.”

8. **S3 — change. Fix both Claude output transports.** Structured tracked runs still use stream-json (`src/delegate_agent/argv_builders.py:259-265`), whose result handler reads only `result` (`src/delegate_agent/harness_events.py:937-948`). Changing `_parse_claude_call_json` alone leaves workflow safe/work stages dependent on the undocumented text echo. **Plan edit:** “Use one small Claude result-text extractor in JSON-call and stream-result parsing: serialize a present valid `structured_output` value, otherwise use string `result`. Preserve `is_error` and pure-mode permission-denial checks independently of extraction. Test both transports, including structured-only results.”

9. **E1–E3/E8/I1 — change. Keep diagnostics separate from assistant output and define recovery semantics.** Generic fallback records a `text` event, not assistant text or a failure (`src/delegate_agent/harness_events.py:530-539`); failure classification reads only `error` and failed terminal events (`src/delegate_agent/runner.py:3082-3095`). Thus E3 makes the audit’s error visible without fixing its false-success outcome. Its blanket fallback also removes a deliberate malformed-tool-output protection (`src/delegate_agent/harness_events.py:507-509`). Separately, any terminal starts the child shutdown grace period (`src/delegate_agent/runner.py:2271-2276,2414-2425`). **Plan edit:** “Preserve bounded, redacted malformed-stream diagnostics without promoting raw tool envelopes to answers. Define when malformed transport makes the result incomplete, and when later valid recovery clears it. Merge E8/I1 into one end-to-end observability item; bound unknown-type cardinality and reuse existing no-assistant-text quality/warning machinery (`runner.py:998-1004,4805-4811`). Test error→EOF and error→successful recovery.”

10. **B4/T1 — change/add. The timeout backstop is optional, and Cursor’s watchdog has the same dispatch defect.** Standalone tracked runs have no deadline unless a timeout was supplied (`src/delegate_agent/runner.py:3658`), so disabling Kimi/Devin stall detection can create unbounded runs. Kimi L5 explicitly calls for an explicit timeout. Cursor’s real started **and completed** `tool_call` events both enter the watchdog’s generic start branch (`src/delegate_agent/stall_watchdog.py:358-375`), leaving pending tools indefinitely. **Plan edit:** “Disable the engine default stall detector only with a finite timeout policy; preserve explicit operator settings. Add Cursor `(type, subtype)`/`call_id` start-finish handling to B4. Move Cursor B2’s parser correction from T1 into E, with matching watchdog tests.”

11. **B3 — change. An expanded allowlist does not implement non-Codex cooldowns.** The writer hardcodes `"codex"` (`src/delegate_agent/runner.py:3136-3147`), and the preflight checks are guarded by `ctx.engine == "codex"` and Codex-specific identities (`:3738-3750`). No non-Codex callers become active when `_VALID_TOOLS` changes. **Plan edit:** “Either implement engine/auth-identity-scoped cooldown write/check integration and test repeated non-Codex launches, or explicitly defer shared L5. Delete the allowlist-only item; it cannot claim this finding fixed.”

12. **R4/T2 — change; merge these items.** Removing dead Grok declaration branches does not stop a Grok cache row from invalidating the entire cache: validation rejects it before resolution (`src/delegate_agent/reasoning.py:1412-1423`). Also, deleting the full declaration path would remove working config/discovery overrides, not merely dead cache behavior (`:615-638`). **Plan edit:** “Accept Grok rows in cache validation without changing unrelated routing tables; preserve config→exact discovery→cache precedence. In the same item replace the duplicated lookup loop with `_lookup_declaration`. Test a mixed Codex/Droid/Grok cache.” Remove the alternative deletion remedy from R4.

13. **A6/R3 — change. Updating fallback enums alone leaves supported efforts unreachable.** Claude discovery overrides the static enum (`src/delegate_agent/reasoning.py:574-585`), while its probe parses the warning that omits `ultracode` (`src/delegate_agent/harness_discovery.py:1411-1420`; Claude audit L1). OMP alias validation independently uses `PI_THINKING_LEVELS` (`src/delegate_agent/config.py:1151-1154`), so adding OMP `auto` only to the native effort resolver is incomplete. **Plan edit:** “R owns all effort vocabulary changes, including engine-specific alias validation. Verify Claude `ultracode` with populated discovery as well as no discovery; add a narrowly evidenced compatibility exception or defer it pending stronger acceptance proof. Test OMP `auto` through CLI and alias paths while Pi continues rejecting it.”

14. **A7 — change. Inspect trailing argv tokens, not the first prompt token.** The reported failure is `"x" --model …`, whose first prompt token is `"x"`. The parser already retains the trailing tokens, builds `known_options`, and respects an explicit `--` boundary (`src/delegate_agent/cli_parser.py:2114-2147`). **Plan edit:** “Warn on recognized option tokens in `checked_prompt_parts`, excluding content after the literal separator. Reuse the existing option set; do not scan joined prompt prose or stdin/file content. Test `"x" --model …`, a quoted prose mention, and explicit `--`.”

15. **A1/A2/A5 — keep/change. Keep stdin migration; narrow the extra flags and strengthen acceptance.** Cursor stdin already has a live audited success under `--mode ask` (`docs/audits/2026-09-07-harness-compat/cursor.md`, S1), so two separate paid mode-selection experiments are unnecessary. OMP’s home-directory redirection is specifically an OMP behavior (`omp.md:50-57`); local inspection of Pi’s shipped argument parser found no `--cwd` option, consistent with its unknown-long-flag behavior (`pi.md:38-51`). **Plan edit:** “Use the already-evidenced Cursor read-only mode unless a concrete requirement favors plan. Pass `--cwd` only to OMP. Verify stdin on existing Cursor followup/structured-resume paths, not only a fresh call. Include the existing OMP read-only behavioral probe; a yolo work smoke does not verify its safe boundary.”

16. **R5 — change/add. Reconcile contradictory catalogs; do not turn account absence into global retirement.** Shared reports Spark present and GPT-5.4 absent, while Codex reports the opposite (`docs/audits/2026-09-07-harness-compat/shared.md:249-258`; `codex.md:105-115`). The shared report also attributes missing OMP providers to account access, not invalid IDs. OMP L5’s fuzzy substitution risk is more consequential than refreshing advisory tables, and `_omp_request_parts` forwards the selected ID without a catalog-membership warning (`src/delegate_agent/request_build.py:3278-3287,3319-3338`). **Plan edit:** “Reconcile catalog captures by selector/profile before deleting bundled rows; absence on one account is not retirement. Add a warning for a resolved OMP selector absent from an available fresh account catalog. Do not rewrite operator aliases or hard-reject models when discovery is unavailable.”

17. **Kimi L2 — add an explicit disposition.** The plan leaves supported `/goal` work pass-through losing `goal.summary`, including its blocked/paused reason (`docs/audits/2026-09-07-harness-compat/kimi.md:62-73`). The typed-event dispatcher drops that event (`src/delegate_agent/harness_events.py:637-640`). This is existing-path outcome reporting, not new resume capability. **Plan edit:** “Preserve bounded `goal.summary` status/reason/accounting for Kimi; retain nonzero failure semantics unless the exact blocked/paused mapping is established. Test the documented payload. If deferred, name this existing-path reporting gap explicitly rather than claiming all outcome-affecting latent findings fixed.”

18. **P1 — change, design recommendation. Keep the switch with tracked-run prompt policy.** I would ship `"tracking": {"skillReviewPreamble": {"enabled": false}}`, alongside `tracking.completionReport`, rather than introduce a top-level `prompt` section for one tracked-run switch. The existing preamble excludes calls and slash commands (`src/delegate_agent/request_build.py:660-670`); completion-report defaults already live under tracking (`src/delegate_agent/config.py:72-76,1689-1697`). A boolean `enabled` fits this switch; copying `defaultMode` would invent unnecessary modes. **Plan edit:** replace P1’s config shape accordingly; preserve call/slash/pass-through exclusions, authored prompt text, safety prefixes, and mail behavior. Keep the decision at request assembly, as the dirty patch does (`request_build.py:3512-3515`), rather than making the formatter load config. Explicitly scope “prefix absent” assertions to automatic injection.

19. **Waves/ownership — change. The lanes are not file-disjoint.** A and B both own `request_build.py`; A6 edits `reasoning.py` owned by R and needs discovery capability data owned by D. R1’s help strings belong to A-owned `command_help.py` and `describe_payload.py`. P already dirties `config.py`/`request_build.py`; the corrected S3 additionally touches E’s event file. **Plan edit:** “Land P first and branch all lanes from that checkpoint. Assign all `request_build.py` writes to one owner, all reasoning/help vocabulary edits explicitly, and discovery capability additions to D. Give S’s stream-result change to E via a declared helper contract. Name test-file ownership too. Merge according to these dependencies, not file size.”

20. **T1/T2 and verification — drop/simplify. Separate required fixes from optional refactoring.** T1 hides a required Cursor fix inside a supposedly behavior-preserving wave; the rest of the large table conversions are not needed to repair the identified outcomes. The shared audit expressly warns that several superficially similar handlers are different state machines (`docs/audits/2026-09-07-harness-compat/shared.md:211-230`). The proposed per-merge gate also runs pytest and Ruff twice because `tests/acceptance.sh:38-41` already runs all four checks. **Plan edit:** “Move required fixes into E/R; retain only small demonstrated deduplications, including R4’s lookup reuse. Defer the broad table conversions and remove line-deletion targets. Run focused checks per lane and the acceptance script once per merged checkpoint; rerun after actual changes or failures.”
