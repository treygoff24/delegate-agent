# D2 findings ledger

Statuses: **fixed** · **adjudicated-away** · **deferred** · **already covered**.

| # | Lane | Finding | Disposition |
|---|---|---|---|
| 1 | refuter (SEV-1), conventions, 3am-ops | `WorkflowState` is constructed in three places and only one carried `notify_target`. A gated sub-workflow wrote `paused` with `notify: null`, and because status.json is rebuilt rather than merged, the child erased the target for the whole run — so `workflow approve` resumed a supervisor that rang nobody. | **fixed** — child and dry-run states inherit it. This is the exact question I put in the brief ("what else does the rebuild erase") and the answer was yes. |
| 2 | refuter (SEV-1), conventions | `replay_journal` had the same problem: a child state defaulted it back to True, silently re-enabling journal replay after a dry run. | **fixed** — inherited alongside notify. I did not find this one; the panel did, by taking the question further than I had. |
| 3 | refuter (SEV-1) | My commit claimed every parse-time option error names `delegate dry-run`. False — only the sites I patched did. `--pass-through` with `--json`, missing option values, and others had nothing. | **fixed by scoping rather than widening**, which is the opposite of what refuter proposed, on conventions' evidence (see 4). The claim in the commit message was an overclaim and is corrected here. |
| 4 | conventions | The hint's own advice fails. "Prefix it with `delegate dry-run`" produces `misplaced_global_option` whenever a global option is present, because globals must precede the subcommand — reproducing the exact frustration the cut recorded, inside its own remediation. | **fixed** — reworded to `delegate [global options] dry-run <engine> <mode> ...` with the ordering stated. This finding also settles the refuter/conventions disagreement about whether to make the hint universal: universal would have made it wrong more often. |
| 5 | refuter (SEV-2) | A corrected command dropped the `dry-run` the caller typed, so validating an invocation handed back a command that **launches**. | **fixed** — corrections preserve `dry-run`, with a test that re-parses the correction and asserts `launch.dry_run`. The most dangerous finding of the wave. |
| 6 | refuter (SEV-2) | `notify_event` discarded the `NotifyOutcome`, so an undelivered notification was indistinguishable from a delivered one — the same failure the feature exists to prevent, one level up. | **fixed** — recorded as a `notify_degraded` journal event with reason and detail. Fixing it introduced a regression (see 7). |
| 7 | my own tests | Recording the degradation through `append_event` reset the workflow's status to `running`, because that helper writes status beside every journal line. Two existing notify tests caught it. | **fixed** — a journal-only append for anything recorded after a terminal write. Worth naming: the tests earned their keep within an hour of being written. |
| 8 | conventions, 3am-ops | The two `agent_timeout` sites still disagreed after my fix, and `agent_structured_retry` was a third impoverished row with the same defect. | **fixed** — both timeout rows carry every identifier each can honestly produce (`engine` stays off the adoption row because it belongs to the adopted run, not that call), and the retry row carries key and label. |
| 9 | test-skeptic | `test_notify_target_is_parsed_and_rejected_early` was decoration: `parse_notify_target` runs before the subcommand allow-list, so a garbage target already failed on the parent. The test would have been green through the entire defect. | **fixed** — replaced with one that asserts a **valid** target is accepted for `workflow run`, which is the behaviour that was broken. Third vacuous test I have written in three waves; the pattern is asserting presence instead of discrimination. |
| 10 | test-skeptic | My hint silently degraded an existing test. `test_ambiguous_prompt_source_includes_corrected_command` split on "Corrected command: " and stripped one trailing period, which after the hint peeled the HINT's period and fed its words into the re-parsed command. It stayed green while checking less. | **fixed** — cuts at the sentence boundary and asserts the re-parsed invocation is the caller's, with no advice smuggled in as prompt text. The insidious class: a change that weakens a test without failing it. |
| 11 | conventions | No CHANGELOG entries and no mention in `docs/delegate-workflows.md`. | **fixed** — Added/Fixed bullets plus a documented section on when notify fires, that the target survives resume, and that it degrades. |
| 12 | refuter, conventions | Both lanes had `uv` blocked in the sandbox and could not run the gate; all claims marked static. | **deferred** — panel infrastructure, same genus as the isolation-disclosure work. Both still found real defects, including the two most dangerous ones. |
